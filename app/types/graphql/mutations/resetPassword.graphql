mutation ResetPassword($token: String!, $password: String!) {
  resetPassword(token: $token, password: $password) {
    __typename
    ... on SuccessPayload {
      success
      message
    }
    ... on ErrorPayload {
      success
      message
      statusCode
    }
  }
}
