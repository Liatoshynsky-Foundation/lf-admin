mutation DeleteBlob(
    $folderName: String!
    $blobName: String!
) {
    deleteBlob(
        folderName: $folderName
        blobName: $blobName
    ) {
        success
        message
        blobName
    }
}