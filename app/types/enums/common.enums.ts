export enum PageStatus {
  Draft = 'draft',
  Published = 'published'
}

export enum NewsStatus {
  Draft = 'draft',
  Published = 'published',
  Hidden = 'hidden',
  Archived = 'archived',
  Editing = 'editing'
}
