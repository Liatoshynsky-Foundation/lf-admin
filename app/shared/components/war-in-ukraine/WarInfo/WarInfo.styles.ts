import { SxProps, Theme } from '@mui/material';

export const styles = {
  fieldsWrapper: {
    display: 'flex',
    flexDirection: 'column',
    gap: '16px',
    marginTop: '15px'
  }
} satisfies Record<string, SxProps<Theme>>;