import { SxProps, Theme } from '@mui/material';

export const styles = {
  imageWrapper: {
    marginTop: '15px'
  },
  quoteWrapper: {
    marginTop: '15px'
  },
  skeletonPlaceholder: {
    height: '60px'
  }
} satisfies Record<string, SxProps<Theme>>;
